<Page
    x:Class="WordTes.UI.Pages.TestSetupPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:src="using:WordTes.UI"
    xmlns:models="using:WordTes.UI.Models"
    mc:Ignorable="d">

    <Page.Resources>
        <models:TestSetupPageModel x:Key="PageModel" />
    </Page.Resources>

    <Grid DataContext="{StaticResource ResourceKey=PageModel}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="80*"/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>

            <TextBlock 
                Text="{StaticResource AppName}" 
                Style="{StaticResource HeaderTextBlockStyle}" 
                Grid.Row="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"/>

            <ScrollViewer HorizontalScrollBarVisibility="Auto" Grid.Row="1">
                <ItemsControl ItemsSource="{ Binding Path=Items, Mode=TwoWay }">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="10,25,15,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="45*"/>
                                    <ColumnDefinition Width="45*"/>
                                    <ColumnDefinition Width="10*"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Margin="10" Text="{Binding Word, Mode=TwoWay}" />
                                <TextBox Margin="10" Grid.Column="1" Text="{Binding Translation, Mode=TwoWay}" />
                                <Button Grid.Column="2" 
                                        CommandParameter="{Binding}"
                                        Command="{Binding Path=RemoveCommand, Source={StaticResource PageModel}}"
                                        Background="#FF0D6AA3" 
                                        Margin="10" 
                                        Height="80" 
                                        Width="80" >
                                    <Image Source="Assets/delete.png"  Stretch="Fill" />
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <Grid Grid.Row="2" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Content="Add" 
                        HorizontalAlignment="Stretch" 
                        FontSize="24"
                        Margin="10"
                        Grid.Column="0" 
                        Command="{Binding Path=AddCommand}"
                        VerticalAlignment="Top"
                        Height="50"/>
                <Button Command="{Binding StartTestCommand}"
                        Content="Begin Test" 
                        HorizontalAlignment="Stretch" 
                        FontSize="24"
                        Margin="10"
                        Grid.Column="1" 
                        VerticalAlignment="Top"
                        Height="50"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
